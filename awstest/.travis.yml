language: java
jdk: oraclejdk8
install: true
  
branches:
  only:
    - master

# Travis CI 서버의 Home
cache:
  directories:
    - C:/Users/jwkim/.m2

script: ./mvnw clean install

# CI 실행 완료시 메일로 알람
notifications:
  email:
    recipients:
      - ghbnty0202@gmail.com
      
language: ruby
install:
  - gem uninstall bundler
  - gem install bundler --version '1.7.4'
script: "rake"