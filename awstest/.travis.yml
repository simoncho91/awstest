language: java
jdk:
  - openjdk8

before_install:
  - gem install bundler --pre

branches:
  only:
    - master

# Travis CI 서버의 Home
cache:
  directories:
    - 'C:/Users/jwkim/.m2/repository'
    - 'C:/Users/jwkim/.maven'

script: "./maven clean build"

# CI 실행 완료시 메일로 알람
notifications:
  email:
    recipients:
      - ghbnty0202@gmail.com 